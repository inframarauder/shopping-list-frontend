import React from "react";
import { Navbar } from "react-bootstrap";

const Header = () => {
	return (
		<Navbar className="header center-content">
			<h4 className="text-white">Shopping List</h4>
		</Navbar>
	);
};

export default Header;
